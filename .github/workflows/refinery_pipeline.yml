name: Alpha-Data-Refinery-Global-V6

on:
  workflow_dispatch:
    inputs:
      manual_market:
        description: '選擇手動執行的市場 (選 all 則執行六國)'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - tw_stock_warehouse
          - us_stock_warehouse
          - jp_stock_warehouse
          - hk_stock_warehouse
          - kr_stock_warehouse
          - cn_stock_warehouse
  schedule:
    - cron: '0 21 * * *'

jobs:
  refine-markets:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 決定執行的市場列表
        market_db: ${{ fromJSON( github.event.inputs.manual_market == 'all' || github.event_name == 'schedule' && '["tw_stock_warehouse", "jp_stock_warehouse", "us_stock_warehouse", "cn_stock_warehouse", "kr_stock_warehouse", "hk_stock_warehouse"]' || format('["{0}"]', github.event.inputs.manual_market) ) }}
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy google-api-python-client google-auth python-dotenv requests google-generativeai

      - name: Run Pipeline
        env:
          GDRIVE_SERVICE_ACCOUNT: ${{ secrets.GDRIVE_SERVICE_ACCOUNT }}
          # 確保 AI 摘要功能有 API Key (若 core_engine 有用到)
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }} 
        run: |
          # 提取簡稱並轉大寫 (例如: tw_stock_warehouse -> TW)
          export MARKET_TYPE=$(echo "${{ matrix.market_db }}" | cut -d'_' -f1 | tr '[:lower:]' '[:upper:]')
          echo "🚀 啟動任務: $MARKET_TYPE"
          python main_pipeline.py

      - name: Upload Summary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: summary-${{ matrix.market_db }}
          # 🚀 使用萬用字元確保抓到產出的 summary 文字檔
          path: "summary_*.txt"
          retention-days: 1

  report-summary:
    needs: refine-markets
    runs-on: ubuntu-latest
    # 即使部分市場精煉失敗，仍嘗試發送現有的報告
    if: always() 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies for Reporter
        run: |
          python -m pip install --upgrade pip
          pip install requests python-dotenv pandas

      - name: Download All Summaries
        uses: actions/download-artifact@v4
        with:
          # 🚀 下載所有以 summary- 開頭的 Artifact 並合併到當前目錄
          pattern: summary-*
          merge-multiple: true

      - name: Verify Files
        run: |
          echo "🔎 目前下載到的摘要檔案清單："
          ls -R summary_*.txt || echo "❌ 找不到任何摘要檔案"

      - name: Send Final Telegram Report
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python batch_reporter.py
